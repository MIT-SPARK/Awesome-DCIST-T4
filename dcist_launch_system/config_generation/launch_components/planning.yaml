#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.all, expects="1+"
---
windows:
#@overlay/append
- window_name: planning
  shell_command_before: source ${DCIST_WS}/install/setup.zsh
  layout: tiled
  panes:
    - source ~/dcist_env/bin/activate && ros2 launch dcist_launch_system master.launch.yaml conf_name:=$config launch_omniplanner:=true
    #! - plan commander repl
