#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.all, expects="1+"
---
map_frame: $<env robot_name>/odom 
robot_id: 1
backend:
  optimize_on_lc: false
  pgmo:
    add_initial_prior: false
