#!/usr/bin/env python3

import click
import subprocess
import shutil


@click.command()
def main():
    cmd = [shutil.which("tmuxp"), "load"]
    return subprocess.run(cmd).returncode


if __name__ == "__main__":
    main()
